import PackagesList from '@/components/PackagesList';

export const metadata = {
  title: 'Tour Packages',
  description: 'Discover and book amazing tour packages from trusted operators.',
};

export default function PackagesPage() {
  return <PackagesList />;
}